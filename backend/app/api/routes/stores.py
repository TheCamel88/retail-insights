"""Store management routes."""
from fastapi import APIRouter
router = APIRouter()

# TODO: GET  /          → list stores for current org
# TODO: POST /          → create store
# TODO: GET  /{store_id} → store detail
